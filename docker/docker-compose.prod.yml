services:
  psql:
    container_name: psql
    image: postgres:17-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
    env_file:
      - ./../api/environments/.env.development
    networks:
      - db_api_net
    healthcheck:
      test: ["CMD-SHELL", 'pg_isready -d $POSTGRES_DB -U $POSTGRES_USER']
      interval: 10s
      timeout: 5s
      retries: 2
  # ====================================
  redis_broker:
    container_name: redis_broker
    image: redis
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - redis_api_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 2
  # ====================================
  api:
    container_name: api
    build:
      context: ./../api/  
      dockerfile: ./../docker/Dockerfile.prod 
    env_file:
      - ./../api/environments/.env.production
    command: ["sh", "-c", "commands.sh"]
    depends_on:
      psql:
        condition: service_healthy
      redis_broker:
        condition: service_healthy
    networks:
      - db_api_net
      - redis_api_net
      - nginx_network
    healthcheck:
      test: ["CMD", "sh", "-c", "grep -q 'MIGRATED=True' /home/djuser/my_environment"]
      interval: 10s
      timeout: 5s
      retries: 2
  # ====================================
  nginx:
    container_name: nginx
    build:
      context: ./../api/  
      dockerfile: ./../docker/Dockerfile.ngx 
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
    networks:
      - nginx_network
  # ====================================
  celery_worker:
    container_name: celery_worker
    build:
      context: ./../api/  
      dockerfile: ./../docker/Dockerfile.prod  
    command: ["sh", "-c", "celery -A config worker --loglevel=debug"]
    env_file:
      - ./../api/environments/.env.production
    depends_on:
      api:
        condition: service_healthy
    networks:
      - db_api_net
      - redis_api_net
    healthcheck:
      test: ["CMD", "celery", "-A", "config", "status"]
      interval: 10s
      timeout: 5s
      retries: 2
  # ====================================
  celery_beat:
    container_name: celery_beat
    build:
      context: ./../api/  
      dockerfile: ./../docker/Dockerfile.prod 
    command: ["sh", "-c", "celery -A config beat --scheduler django --loglevel=debug"]
    env_file:
      - ./../api/environments/.env.production
    depends_on:
      celery_worker:
        condition: service_healthy
    networks:
      - db_api_net
      - redis_api_net
# ====================================
volumes:
  postgres_data:
  redis_data:
# ====================================
networks:
  db_api_net:
    driver: bridge
  redis_api_net:
    driver: bridge
  nginx_network:
    driver: bridge
